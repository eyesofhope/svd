<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="webTab"
            type="com.myAllVideoBrowser.ui.main.home.browser.webTab.WebTab" />

        <variable
            name="tabListener"
            type="com.myAllVideoBrowser.ui.component.adapter.WebTabsListener" />

    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/item_web_tab_button"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginVertical="4dp"
        android:onClick="@{(v) -> tabListener.onSelectTabClicked(webTab)}"
        app:cardBackgroundColor="@color/brand_accent_soft"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingEnd="4dp">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/faviconTab"
                android:layout_width="32dp"
                android:layout_height="32dp"
                app:bitmap="@{webTab.favicon}"
                tools:src="@drawable/public_24px" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tab_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:paddingStart="12dp"
                android:paddingEnd="4dp"
                android:text="@{webTab.title}"
                android:textColor="?attr/colorOnSurface"
                android:textSize="@dimen/text_medium"
                android:textStyle="bold"
                tools:text="Homepage" />

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/incognitoBadge"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="6dp"
                android:contentDescription="@string/incognito_badge"
                android:visibility="@{webTab.isIncognito ? View.VISIBLE : View.GONE}"
                app:srcCompat="@drawable/ic_incognito_24"
                app:tint="?attr/colorOnSurface" />

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/closeTab"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:onClick="@{(v) -> tabListener.onCloseTabClicked(webTab)}"
                android:padding="10dp"
                android:src="@drawable/close_24px"
                app:tint="?attr/colorOnSurface" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
